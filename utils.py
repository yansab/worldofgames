scores_file_name = 'scores.txt'  # a string representing a file name, by default “Scores.txt”.
bad_return_code = 200  # a number representing a bad return code for a function.


def screen_cleaner():  # a  function to clear the screen (before a new game starts).
    print('\n' * 50)
